apiVersion: v1
kind: Service
metadata:
  name: ultraportal-backend-service
  namespace: ultraportal
spec:
  selector:
    app: ultraportal-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort
  nodePort: 30080
